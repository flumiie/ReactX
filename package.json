{
  "scripts": {
    "app:build": "cd app && yarn start:android",
    "app:start": "cd app && yarn start",
    "app:log": "cd app && yarn log",
    "app:shake": "cd app && yarn shake",
    "gql:start": "cd graphql && yarn start",
    "preinstall": "cd app && yarn && cd ../graphql && yarn"
  },
  "dependencies": {
    "apollo-server": "^2.9.12",
    "node-fetch": "^2.6.0"
  }
}
